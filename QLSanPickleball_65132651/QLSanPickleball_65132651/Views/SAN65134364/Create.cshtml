@model QLSanPickleball_65132651.Models.SAN

@{
    ViewBag.Title = "Thêm sân";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<style>
    .create-wrapper {
        max-width: 980px;
        margin: 0 auto;
    }

    .page-header {
        margin-bottom: 18px;
    }

        .page-header h2 {
            margin: 0;
            font-size: 24px;
            font-weight: bold;
            color: #111827;
        }

    .form-card {
        background: white;
        border-radius: 14px;
        border: 1px solid #e5e7eb;
        box-shadow: 0 2px 8px rgba(0,0,0,0.06);
        padding: 24px;
    }

    .form-grid {
        display: grid;
        grid-template-columns: 260px 1fr;
        gap: 28px;
    }

    .court-preview {
        border: 1px solid #e5e7eb;
        border-radius: 16px;
        padding: 22px;
        background: #f9fafb;
        text-align: center;
        height: fit-content;
    }

    .court-circle {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        background: linear-gradient(135deg, #16a34a, #065f46);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 42px;
        font-weight: bold;
        margin: 0 auto 16px auto;
        box-shadow: 0 8px 18px rgba(22, 163, 74, 0.25);
    }

    .court-title {
        font-size: 18px;
        font-weight: bold;
        color: #111827;
        margin-bottom: 6px;
    }

    .court-note {
        color: #6b7280;
        font-size: 13px;
        line-height: 1.5;
    }

    .info-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 18px;
    }

    .form-group-full {
        grid-column: 1 / span 2;
    }

    .form-label {
        display: block;
        font-weight: bold;
        color: #374151;
        margin-bottom: 6px;
    }

    .form-control,
    .form-select {
        width: 100%;
        padding: 10px 12px;
        border: 1px solid #d1d5db;
        border-radius: 8px;
        outline: none;
        background: white;
    }

        .form-control:focus,
        .form-select:focus {
            border-color: #16a34a;
            box-shadow: 0 0 0 2px rgba(22, 163, 74, 0.12);
        }

    textarea.form-control {
        min-height: 110px;
        resize: vertical;
    }

    .text-danger {
        color: #dc2626;
        font-size: 13px;
        margin-top: 4px;
        display: block;
    }

    .action-bar {
        margin-top: 24px;
        display: flex;
        gap: 10px;
        justify-content: flex-end;
        flex-wrap: wrap;
    }

    .btn-save {
        background: #16a34a;
        color: white;
        border: none;
        padding: 11px 18px;
        border-radius: 8px;
        font-weight: bold;
        cursor: pointer;
    }

        .btn-save:hover {
            background: #15803d;
        }

    .btn-back {
        background: #6b7280;
        color: white !important;
        padding: 11px 18px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: bold;
    }

        .btn-back:hover {
            background: #4b5563;
            text-decoration: none;
            color: white;
        }

    .validation-summary-errors {
        background: #fee2e2;
        color: #991b1b;
        border: 1px solid #fecaca;
        border-radius: 8px;
        padding: 12px 16px;
        margin-bottom: 18px;
    }

    @@media(max-width: 850px) {
        .form-grid {
            grid-template-columns: 1fr;
        }

        .info-grid {
            grid-template-columns: 1fr;
        }

        .form-group-full {
            grid-column: 1;
        }
    }
</style>

<div class="create-wrapper">

    <div class="form-card">
        @using (Html.BeginForm("Create", "SAN65134364", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

            <div class="form-grid">
                <div class="court-preview">
                    <div class="court-circle" id="courtPreview">S</div>
                    <div class="court-title" id="courtNamePreview">Sân mới</div>
                    <div class="court-note">
                        Mã sân có thể để trống để hệ thống tự tạo, ví dụ S06.
                    </div>
                </div>

                <div>
                    <div class="info-grid">
                        <div class="form-group">
                            <label class="form-label">Mã sân</label>
                            @Html.TextBoxFor(model => model.MASAN, new
                            {
                                @class = "form-control",
                                placeholder = "Để trống để tự tạo, ví dụ S06",
                                id = "MASAN"
                            })
                            @Html.ValidationMessageFor(model => model.MASAN, "", new { @class = "text-danger" })
                        </div>

                        <div class="form-group">
                            <label class="form-label">Tên sân</label>
                            @Html.TextBoxFor(model => model.TENSAN, new
                            {
                                @class = "form-control",
                                placeholder = "Nhập tên sân",
                                id = "TENSAN"
                            })
                            @Html.ValidationMessageFor(model => model.TENSAN, "", new { @class = "text-danger" })
                        </div>

                        <div class="form-group">
                            <label class="form-label">Loại sân</label>
                            @Html.DropDownList("MALOAISAN",
                                null,
                                "-- Chọn loại sân --",
                                new { @class = "form-select" })
                            @Html.ValidationMessageFor(model => model.MALOAISAN, "", new { @class = "text-danger" })
                        </div>

                        <div class="form-group">
                            <label class="form-label">Trạng thái sân</label>
                            @Html.DropDownListFor(model => model.TRANGTHAISAN,
                                (SelectList)ViewBag.TrangThaiList,
                                "-- Chọn trạng thái --",
                                new { @class = "form-select" })
                            @Html.ValidationMessageFor(model => model.TRANGTHAISAN, "", new { @class = "text-danger" })
                        </div>

                        <div class="form-group form-group-full">
                            <label class="form-label">Mô tả sân</label>
                            @Html.TextAreaFor(model => model.MOTASAN, new
                            {
                                @class = "form-control",
                                placeholder = "Nhập mô tả sân, vị trí sân, ghi chú..."
                            })
                            @Html.ValidationMessageFor(model => model.MOTASAN, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="action-bar">
                        @Html.ActionLink("Quay lại", "Index", "SAN65134364", null, new { @class = "btn-back" })
                        <button type="submit" class="btn-save">Lưu sân</button>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<script>
    const inputMaSan = document.getElementById("MASAN");
    const inputTenSan = document.getElementById("TENSAN");
    const courtPreview = document.getElementById("courtPreview");
    const courtNamePreview = document.getElementById("courtNamePreview");

    function capNhatPreviewSan() {
        let maSan = inputMaSan.value.trim();
        let tenSan = inputTenSan.value.trim();

        courtPreview.innerText = maSan.length > 0 ? maSan.toUpperCase() : "S";
        courtNamePreview.innerText = tenSan.length > 0 ? tenSan : "Sân mới";
    }

    inputMaSan.addEventListener("input", capNhatPreviewSan);
    inputTenSan.addEventListener("input", capNhatPreviewSan);
</script>